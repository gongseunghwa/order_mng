CREATE TABLE tb_lm_item (
                            item_id BIGINT PRIMARY KEY,
                            item_nm VARCHAR(255),
                            item_price BIGINT,
                            item_inventory BIGINT
);
